---
:backends:
  - yaml
  - json

:hierarchy:
  - "level1/%{::name}"
  - "level2/%{name}"
  - "level3/%{name}"
  - "level4/%{name}"
  - "level1/common"
  - "level2/common"

:yaml:
  :datadir: data

:json:
  :datadir: data
